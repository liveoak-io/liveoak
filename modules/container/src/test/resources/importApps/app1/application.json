{
  name: 'app1',
  html-app: '/app/index.html',
  resources : {
    app: {
      type: 'dummy',
      config: {
        directory: "${application.dir}/app/"
      }
    },

    client: {
      type: 'dummy',
      config: {
        directory: "${io.liveoak.js.dir}"
      }
    }
  }
}
