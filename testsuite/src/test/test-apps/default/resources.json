
{
  app: {
    type: 'filesystem',
    config: {
      directory: '${application.dir}/app'
    }
  },

  client: {
    type: 'aggregating-filesystem',
    config: {
      directory: "${io.liveoak.js.dir}"
    }
  }
}
